version: '3'
services:
  db:
    container_name: mysql
    image: mysql:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: ismat
      MYSQL_PASSWORD: secret
    ports:
      - 3306:3306